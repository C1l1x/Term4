;Да се дефинира функция, която по зададено цяло
;положително число n, проверява дали то е палиндром.

(defvar n)

(defun Num (n)
    (loop
     (let ((rnum 0))
     (cond ((> n 0) (setq rnum (mod n 10)))
            
           )
     )
    ))

(fresh-line)